export interface DefaultColumns {
  lift: string;
  weight: string;
  sets: string;
  reps: string;
}

export interface DefaultRows {
  lift: string;
  weight: string;
  sets: string;
  reps: string;
  difficultly?: string;
}
