{
  "name": "dashboard",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "private": true,
  "scripts": {
    "frontend": "yarn workspace frontend start",
    "backend": "yarn workspace backend start",
    "build:frontend": "lerna run build --scope={@abb/frontend}"
  }
}
